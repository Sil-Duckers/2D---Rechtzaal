using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class BattleSlider : MonoBehaviour
{

    public Slider ScoreSlider;

   /* public void SetSlider(Unit unit)
    {
        ScoreSlider.maxValue = enemyUnit.score + playerUnit.score; // hiermee hoop ik de slider interactief te maken. Ik weet niet of maxValue live updatebaar is
        ScoreSlider.value = enemyUnit.score; // als je dus de score van de ene tegen beide scores hebt zou je mooi een verhouding krijgen
    }

    public void SetScore(int Pscore)
    {
        ScoreSlider.value = Pscore;
    }
   */ 
}
